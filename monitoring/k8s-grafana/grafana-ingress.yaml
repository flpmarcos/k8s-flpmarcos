apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-ui
  namespace: monitoring
spec:
  rules:
  - http:
      paths:
      - pathType: Prefix
        path: "/grafana"
        backend:
          service:
            name: grafana-service
            port:
              number: 3000
